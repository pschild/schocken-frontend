import { PlaceTypeToLabelPipe } from './place-type-to-label.pipe';

describe('PlaceTypeToLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new PlaceTypeToLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
