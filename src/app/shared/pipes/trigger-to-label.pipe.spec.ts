import { TriggerToLabelPipe } from './trigger-to-label.pipe';

describe('TriggerToLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new TriggerToLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
