import { AutoPlacementDirective } from './auto-placement.directive';

describe('AutoPlacementDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoPlacementDirective();
    expect(directive).toBeTruthy();
  });
});
