import { ContextToLabelPipe } from './context-to-label.pipe';

describe('ContextToLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ContextToLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
