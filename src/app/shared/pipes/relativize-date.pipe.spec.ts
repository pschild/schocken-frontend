import { RelativizeDatePipe } from './relativize-date.pipe';

describe('RelativizeDatePipe', () => {
  it('create an instance', () => {
    const pipe = new RelativizeDatePipe();
    expect(pipe).toBeTruthy();
  });
});
