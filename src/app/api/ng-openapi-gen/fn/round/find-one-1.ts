/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RoundDto } from '../../models/round-dto';

export interface FindOne_1$Params {
  id: string;
}

export function findOne_1(http: HttpClient, rootUrl: string, params: FindOne_1$Params, context?: HttpContext): Observable<StrictHttpResponse<RoundDto>> {
  const rb = new RequestBuilder(rootUrl, findOne_1.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RoundDto>;
    })
  );
}

findOne_1.PATH = '/round/{id}';
